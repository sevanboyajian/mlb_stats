from __future__ import annotations

# Convenience wrapper so you can run: `python calibrate_model.py`
# (implementation lives next to the pipeline in batch/pipeline/)
from batch.pipeline.calibrate_model import main


if __name__ == "__main__":
    main()

